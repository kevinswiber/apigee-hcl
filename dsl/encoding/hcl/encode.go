package hcl

/*
import (
	"github.com/hashicorp/hcl/hcl/printer"
	"io"
	"reflect"
)

// Marshaler is the interface implemented by objects that can
// marshal themselves into valid HCL objects.
type Marshaler interface {
	MarshalHCL(v interface{}) ([]byte, error)
}

// Marshal turns an object into valid HCL.
func Marshal(v interface{}) ([]byte, error) {
}

// Encoder encodes an object in HCL
type Encoder struct {
}

var marshalerType = reflect.TypeOf((*Marshaler)(nil)).Elem()
*/
